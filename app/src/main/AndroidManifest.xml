<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="developer.abdusamid.moviesapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_movie_app"
        android:label="Movie App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoviesApp">
        <activity android:name="moviesapp.Avangers" />
        <activity
            android:name="moviesapp.AddMovie"
            android:label="Add Movie" />
        <activity
            android:name="moviesapp.Edit"
            android:label="Edit Movies" />
        <activity
            android:name="moviesapp.MainActivity"
            android:exported="true"
            android:label="Movies">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>